language: csharp

matrix:
  include:
    - dotnet: 2.1.300
      mono: latest
      env:
        -DOT_NET="Core"
        -FrameworkPathOverride=$TRAVIS_BUILD_DIR

script:
  - if [ "$DOT_NET" == "Core" ]; then dotnet restore; fi
  - if [ "$DOT_NET" == "Core" ]; then dotnet test; fi
